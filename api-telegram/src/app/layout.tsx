/**
 * Минимальный layout для API проекта
 * Не используется для рендеринга, только для структуры Next.js
 */

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
